# Test folder

This folder contains a couple of sample Vitest tests to validate the test setup.

Run the tests from the existing `frontend` workspace (Vitest is configured there):

```powershell
cd frontend
npm test
# or for watch mode:
npm run test:watch
```

Files:
- `sample.test.ts` — tiny unit tests used as examples.
